package com.devquest.domain.skill.repository;

import com.devquest.domain.skill.model.SkillPrerequisiteSkill;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SkillPrerequisiteSkillRepository extends JpaRepository<SkillPrerequisiteSkill, Long> {
}
